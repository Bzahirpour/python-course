# Math module
import math

# We can now call math methods
# Ceiling will round to the next whole number
print(math.ceil(2.9))

# Round function example
x = -2.9
print(round(x))

# Absolute function, the distance from zero (always returns a positive)
print(abs(x))

